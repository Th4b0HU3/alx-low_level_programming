#include "main.h"

/**
 * main - Cheks for lowercase charactor
 * Return: 1 for lowercase charactor or 0 for anything else
 */

int _islower(int c);
{
	typedef int c i32;

	if (c >= 97 && c <= 122)
	{
	return (1);
	}
	return (0);
}
