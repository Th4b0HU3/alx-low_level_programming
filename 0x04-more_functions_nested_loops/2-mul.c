#include "main.h"
#include <stdio.h>
/**
 * mul - multiplies two integers
 * @a: The first integer
 * @b: the econd integer
 * Return: 0 (success)
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
