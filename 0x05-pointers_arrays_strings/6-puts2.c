#include "main.h"
/**
 * puts2 - function that prints every other character of a string
 * starting with the first character
 * @*str - input
 * return: print
 */
void puts2(char *str)
{
	int long = 0;
	int t = 0;
	char *y = str;

	int 0;

	while (*y != '\0')
	{
		y++;
		longi++;
	}
	t = longi - 1;
	_putchar(str[0]);

	_putchar('\n');
}
